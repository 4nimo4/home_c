/*-------------------------------------------------------------------
 * A5.c
 * ------------------------------------------------------------------
 * Подсчет битов
 * ------------------------------------------------------------------
 *На вход программе подается беззнаковое 32-битное целое число N. 
 Требуется найти количество единичных битов в двоичном представлении 
 данного числа.
 * ------------------------------------------------------------------

 */

#include <stdio.h>
#include <inttypes.h>

int main(void)
{
    uint32_t N;    // беззнаковое 32-битное число
    int count = 0; // количество единичных битов

    // Считываем N
    if (scanf("%" SCNu32, &N) != 1) {
        return 1; // ошибка ввода
    }

    // Алгоритм Брайана Кернигана:
    // каждый шаг цикла убирает один установленный бит.
    while (N != 0) {
        N &= (N - 1); // сбрасываем младший установленный бит
        count++;      // считаем, сколько раз это удалось сделать
    }

    // Выводим количество единичных битов
    printf("%d\n", count);

    return 0;
}