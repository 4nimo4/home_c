/*
//------------------------------------------------------
//        Библиотека ncurses.h
//------------------------------------------------------
ncurses.h — это библиотека функций, которая управляет
отображением приложения на терминалах с символьными ячейками.
📟 образует оболочку для работы с необработанными кодами
    терминала
📟 предоставляет функции для перемещения курсора, создания окон,
    создания цветов, игры с мышью и т. д.
    Прикладным программам больше не нужно беспокоиться о базовых
    возможностях терминала.
//------------------------------------------------------
// Особенности библиотека ncurses.h
//------------------------------------------------------
📟 Библиотека не только создаёт оболочку для использования терминала, но и
предоставляет надежную структуру для создания красивого
пользовательского интерфейса в текстовом режиме.
📟 Она предоставляет функции для создания окон, панелей, меню и т. д. Можно
создавать приложения, содержащие несколько окон, меню, панелей и форм.
📟 Все окна могут управляться независимо, можно реализовать «возможность
прокрутки» и даже минимизации окна.
//------------------------------------------------------
// Установка и компиляция Linux и Windows
//------------------------------------------------------
Под Linux: 
Установка командой sudo apt-get install libncursesw5-dev
Внимание! Для компиляции данного примера необходимо добавить ключ -lncurses.
            gcc -o prog main.c -lncurses
Под Windows:
1-ый способ msys-2 установка командой pacman -S ncurses ncurses - MSYS2 Packages
            gcc -o prog main.c -lncurses -DNCURSES_STATIC
how to install ncurses on windows - Stack Overflow
или https://packages.msys2.org/package/ncurses?repo=msys&variant=x86_64
https://stackoverflow.com/questions/75556484/how-to-install-ncurses-on-windows

Установка и компиляция Windows 2-ой способ:
Под Windows:
2-ой способ https://pdcurses.org/ нужно скачать и запустить mingw32-make для сборки в
каталоге \wincon
gcc.exe -Wall -std=c99 -g -ID:\geekbrains.ru\Projects\C\PDCurses-master -c main.c -o main.o
gcc.exe -o pdcurses_test2.exe main.o D:\geekbrains.ru\Projects\C\PDCurses-master\wincon\pdcurses.a
-ID:\geekbrains.ru\Projects\C\PDCurses-master

GitHub - wmcbrine/PDCurses: A curses library for environments that don't fit the termcap/terminfo model.
//------------------------------------------------------
// Hello world! в стиле ncurses
//------------------------------------------------------

1. Установка библиотеки ncurses в Kubuntu
В Kubuntu (и других Debian/Ubuntu) нужна dev‑версия библиотеки:

sudo apt update
sudo apt install libncurses5-dev libncursesw5-dev
libncurses5-dev — основная заголовки и файлы для линковки.
libncursesw5-dev — версия с поддержкой широких символов (UTF‑8).
(Сейчас в новых Ubuntu/Kubuntu пакет libncurses5-dev и libncursesw5-dev
заменён на обобщённый libncurses-dev)
2. Компиляция из терминала (без Makefile)
Находясь в папке с файлом Biblioteka_ncurses_h.c:

gcc -Wall Biblioteka_ncurses_h.c -o prog -lncurses
Разбор команды:
gcc — компилятор.
-Wall — включить все основные предупреждения.
Biblioteka_ncurses_h.c — исходный файл.
-o prog — имя исполняемого файла (будет создан ./prog).
-lncurses — при линковке подключить библиотеку ncurses.
Запуск: ./prog

*/
#include <ncurses.h>   // Подключаем библиотеку ncurses для работы с текстовым интерфейсом в терминале
#include <unistd.h>    // Подключаем unistd.h для функции sleep()

int main() 
{
    // Инициализируем режим работы ncurses.
    // Захватывает экран терминала, подготавливает его для вывода с помощью ncurses.
    initscr();

    // Выводим строку "Hello World !!!" в текущую позицию курсора (по умолчанию в левый верхний угол).
    printw("Hello World !!!");

    // Выводим следующую строку с просьбой нажать клавишу.
    // \n — перевод строки.
    printw("\nPress any key to continue... ");

    // refresh(); 
    // Функция refresh() обновляет физический экран, отображая весь накопленный вывод.
    // В ncurses вывод сначала идет во внутренний буфер, а на экран попадает после refresh().
    // В данном случае вывод всё равно станет виден после getch(), но можно поэкспериментировать:
    // включить и выключить эту строку, чтобы понять разницу.

    // Делаем паузу в 2 секунды перед ожиданием нажатия клавиши.
    sleep(2);

    // Ожидаем нажатия любой клавиши пользователем.
    // Программа "зависает" на этом месте, пока пользователь не нажмет клавишу.
    getch();

    // Завершаем режим ncurses, возвращаем терминал в обычное состояние.
    endwin();

    // Возвращаем 0 — признак успешного завершения программы.
    return 0;
}