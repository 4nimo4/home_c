/*
//------------------------------------------------------
//    Снижение вычислительной нагрузки
//------------------------------------------------------ 
Многие компиляторы автоматически снижают вычислительную нагрузку
путём замены на более легковесные выражения:

x = x / 8;              x = x >> 3;
y = pow(x, 2);          y = x * x;
z = y * 33;             z = (y<<5) + y;

//------------------------------------------------------
//  If else оптимизация
//------------------------------------------------------
● При использовании множественных if, сначала поместите тесты для
ситуаций, которые возникают чаще всего.
● Часто это принимает форму длинной череды взаимоисключающих
if-then-else, из которых выполняется только одно.
● Если на первое место поставить наиболее вероятный вариант, то в
долгосрочной перспективе потребуется выполнять меньше «if».
● Но если условия просты, например if (x==3), рассмотрите возможность
использования оператора switch(x){case 3.
● Некоторые компиляторы довольно хорошо умеют оптимизировать
операторы switch. 

//------------------------------------------------------
//  Кеширующий массив в рекурсии
//------------------------------------------------------
Иногда стоит использовать кеширующий массив вместо рекурсивного
вызова.

int factorial(int i) 
{
    if (i <= 0)
        return 1;
    else
        return i * factorial(i - 1);
}

static int factorial_table[] =b{1, 1, 2, 6, 24, 120, 720 // и так далее };
int factorial(int i)
{
    return factorial_table[i];
}

//------------------------------------------------------
//  Советы новичкам
//------------------------------------------------------
Некоторые вещи, которые могут сбить с толку новичков:
1. Программисты склонны переоценивать полезность написанных ими
программ. Примерная стоимость оптимизации:
<количество запусков> × <количество пользователей> ×
<экономия времени> × <зарплата пользователя> -- < время,
потраченное на оптимизацию> × <зарплата программиста>

Даже если программа будет запускаться тысячами пользователей сотни раз,
дополнительный день, потраченный на экономию 40 миллисекунд, вероятно, не
поможет.

2. Все компьютеры/устройства разные. То, что быстро на одной машине,
может быть медленным на другой. Дополнительные интерфейсные карты,
разные диски, количество зарегистрированных пользователей,
дополнительная память, фоновые демоны и всё остальное могут повлиять на
скорость различных частей программы, на то, какая часть программы
является узким местом, и на её скорость в целом.
Проблема заключается в том, что многие программисты являются опытными
пользователями и имеют компьютеры, загруженные памятью,
математическим сопроцессором и тоннами дискового пространства, в то
время как пользователи получают простые компьютеры и работают по сети.
Программист получает искажённое представление о производительности
программы и может не оптимизировать те части программы, которые
отнимают время пользователя, например подпрограммы с плавающей
запятой или интенсивные операции с памятью.

3. Многие из этих оптимизаций могут быть уже выполнены компилятором!
4. Не берите в привычку писать код в соответствии с приведёнными выше
правилами оптимизации. Применяйте их только после того, как вы точно
определите, какая функция является узким местом. Некоторые правила, если
их применить глобально, сделают программу еще медленнее. Практически
все они делают назначение кода менее очевидным для читателя-человека,
что может доставить неприятности, если вам или кому-то ещё понадобится
исправить ошибку или что-то ещё в дальнейшем. Внимание! Обязательно
прокомментируйте оптимизацию — следующий программист может просто
предположить, что это уродливый код, и переписать его.

5. Неделя, потраченная на оптимизацию программы, легко может стоить
тысячи долларов рабочего времени программиста. Иногда проще просто
купить более быстрый процессор, больше памяти или более быстрый диск и
таким образом решить проблему.
6. Новички часто предполагают, что написание большого количества
операторов в одной строке и удаление пробелов и табуляции ускорит
процесс. Хоть это может быть допустимым методом для некоторых
интерпретируемых языков, в C такой метод не работает. 😊
*/
#include <sys/time.h>     // структуры и функции для работы с временем (struct timeval)
#include <sys/resource.h> // структура rusage и функция getrusage
#include <string.h>       // стандартные POSIX-функции (здесь напрямую не используются)
#include <stdio.h>        // стандартный ввод-вывод (printf и т.п.)

int main(int argc, char **argv) 
{
    
    return 0; // успешное завершение программы
}
