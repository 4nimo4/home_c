/*
 * S1_16_Cikl.c
 * 
	Подсчитать количество натуральных чисел n (102 <= n <= 987), 
	в которых все три цифры различны.  
 * 
 * 
 */

#include <stdio.h>
int main()
{
    int i, count = 0, d1, d2, d3;
    for (i = 102; i <= 987; i++)
		{
			d1 = i % 10;
			d2 = i % 100/10;
			d3 = i / 100; 
			if (d1!=d2 && d1!=d3 && d2!=d3);
//if (i / 100 != i % 10 && i / 100 != i % 100 / 10 && i % 100 / 10 != i % 10)
			count++;            
		}
    printf("%d", count);
    return 0;
}
